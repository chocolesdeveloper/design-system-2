{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@storybook/addon-docs": "^7.0.11",
    "@storybook/cli": "^7.0.10",
    "turbo": "^1.9.3"
  }
}
